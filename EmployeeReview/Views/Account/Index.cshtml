@model IEnumerable<EmployeeReview.Models.Categories>
@using GridMvc.Html
@using EmployeeReview.Models
@using GridMvc.Sorting

@{
    ViewBag.Title = "Index";
}
<h1></h1>
@if (Session["LoggedUserId"] != null)
{
 <text>
 <h1> Welcome @ViewBag.fname @ViewBag.lname !</h1>
 </text>


 @Html.ActionLink("LogOut", "LogOff", "Account");

}
@{
@Html.Grid(Model).Columns(columns =>
           {
               columns.Add(m => m.CategoryValue).Sortable(false).Filterable(true);
               columns.Add(m => m.RatingValue).Sortable(false).Filterable(true);
           })
  
  
  
  //@Html.Grid(Model).AutoGenerateColumns();
      }
 
